name: workflow-1
on: push

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v7

      - name: install dependecies
        run: npm install

      - name: test
        run: npm test

      - name: starting the app
        run: npm start
